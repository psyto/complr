{
  "name": "complr",
  "version": "0.1.0",
  "description": "AI-powered compliance infrastructure for the digital asset industry",
  "private": true,
  "scripts": {
    "prepare": "cd packages/core && pnpm run build",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.31.0"
}
